// ZorroControllerXChainWithdraw contract
// Includes all tests for processing cross chain withdrawals

contract('ZorroControllerXChainWithdraw :: Sending', async accounts => {
    xit('Sends cross chain withdrawal request', async () => {
        /* GIVEN
        - As a public user
        */

        /* WHEN
        - I initiate a cross chain withdrawal
        */

        /* THEN
        - A LayerZero message should be sent with the appropriate payload
        */
    });
});