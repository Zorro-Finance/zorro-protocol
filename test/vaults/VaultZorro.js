const GeneralVault = artifacts.require('MockVaultZorro');

contract('VaultZorro', async accounts => {
    xit('deposits want token', async () => {
        // only Zorro controller
    });

    xit('exchanges USDC for want token', async () => {
        // only Zorro controller
    });

    xit('withdraws want token', async () => {
        // only Zorro controller
    });

    xit('exchanges want token for USD', async () => {
        // only ZOR controller
    });
})