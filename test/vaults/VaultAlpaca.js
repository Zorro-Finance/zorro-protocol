// VaultAlpaca tests
// Tests for Alpaca leveraged lending vaults

contract('VaultAlpaca :: Setters', async accounts => {
    it('Sets lending token (owner only)', async () => {
        /* GIVEN
        */

        /* WHEN
        */

        /* THEN
        */
    });
});

contract('VaultAlpaca :: Investments', async accounts => {
    it('Farms', async () => {
        /* GIVEN
        */

        /* WHEN
        */

        /* THEN
        */
    });

    it('Unfarms', async () => {
        /* GIVEN
        */

        /* WHEN
        */

        /* THEN
        */
    });
});