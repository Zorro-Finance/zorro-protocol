// VaultBaseLiqStakeLP tests
// Tests for all functions common to vaults that offer liquid staking + LP staking

contract('VaultBaseLiqStakeLP :: Setters', async accounts => {
    it('Sets whether LP pool is farmable', async () => {
        /* GIVEN
        */

        /* WHEN
        */

        /* THEN
        */
    });
});