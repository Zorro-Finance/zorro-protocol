const SafeSwapUni = artifacts.require('SafeSwapUni');
const SafeSwapBalancer = artifacts.require('SafeSwapBalancer');

contract('SafeSwapUni', async accounts => {
    it('calculates correct amount out', async () => {
        
    });
});

contract('SafeSwapBalancer', async accounts => {
    it('calculates correct amount out', async () => {
        
    });

    it('calculates correct exchange rate', async () => {
        
    });
});